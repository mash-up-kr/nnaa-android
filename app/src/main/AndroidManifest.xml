<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mashup.nnaa">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".NnaaApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">

        <activity android:name=".RegisterActivity" />
        <!-- 페이스북 메타데이터 -->
        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_key" /> 
        <!-- 카카오톡 메타데이터 -->
        <meta-data
            android:name="com.kakao.sdk.AppKey"
            android:value="@string/kakao_app_key" />


        <activity android:name=".SplashActivity">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".LoginActivity" />
          
        <activity android:name=".SelectFriendActivity" />
        <activity android:name=".main.MainActivity" />
        <activity android:name=".AnswerMultipleChoiceActivity">

        </activity>

        <activity android:name=".FavoritesActivity" />
        <activity android:name=".DeleteQuestion" />
        <activity android:name=".QuestionActivity" />
        <activity android:name=".HowQuestionActivity" />
        <activity android:name=".SetTypeOfFriendActivity" />
        <activity
            android:name=".CustomQuestionActivity"
            android:windowSoftInputMode="adjustResize" />
    </application>

</manifest>
